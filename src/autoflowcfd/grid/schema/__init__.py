"""Grid data schema: SoA-layout dataclasses for nodes, cells, boundaries, faces.

Purely organizational grouping - re-exported at the `autoflowcfd.grid` and
`autoflowcfd.grid.structures` package level, so import from there in
external code rather than from these submodules directly.
"""
